# VbeComplete — VBA 变量名/过程名自动补全工具

在 VBE（VBA 编辑器）代码窗里输入变量/过程名的首字母（或中文首字），自动弹出候选列表，
`Tab` 确认补全，`↑`/`↓` 选择，`Esc` 取消；**鼠标点击其它地方，弹窗会自动消失**。
支持中文命名的变量/常量/过程（如 `Dim 用户名 As String`）。解决 VBE 原生只对"对象.成员"
弹列表、但不对自定变量名弹列表的痛点。

## 技术栈

- **Python** + `pywin32`（驱动 VBA 编辑器 COM）+ `pynput`（全局键盘监听）+ `tkinter`（弹窗）
- 核心解析与补全逻辑是纯 Python，不写任何日志文件、不含测试与调试代码。

## 安装与运行

1. 确保本机已安装 Python 3.8+（建议从 python.org 安装，并勾选"Add to PATH"）。
2. 首次运行会自动 `pip install` 依赖，请**以能联网的普通用户**双击 `run.bat`
   （后台静默运行）。若想看报错，在命令行运行 `python main.py`（带控制台）。
3. 在 Excel 中开启权限（必须）：
   **文件 → 选项 → 信任中心 → 信任中心设置 → 宏设置 →**
   **勾选"信任对 VBA 工程对象模型的访问"**。
4. 打开 VBE（`Alt+F11`），在代码窗输入一个变量名的首字母，即可看到补全列表。

## 使用

- 输入字母/数字/`_` 即弹列表；继续输入会实时过滤。
- **候选列表每条前面带 `1.` `2.` `3.` … 序号**（不足 8 条时用空行把列表撑到 8 行）。
- **按数字键 `1`~`9` 直接选中对应候选并写回编辑器**（最省事，不用再按确认键）。
  数字超过候选条数时不拦截，照常输入数字。小键盘 `1`~`9` 同样可用。
- `↑` / `↓` 选择候选（循环），**选好后按 `Tab` 才写回编辑器**。
- **`Tab` 是唯一确认键**。
  空格和回车按普通输入处理（弹窗会自动收起，然后正常换行 / 输入空格），
  避免误把词写进编辑器。若以后想把回车也加回来：
  把 `main.py` 里的 `CONFIRM_VKS = (VK_TAB,)` 改成 `(VK_TAB, VK_RETURN)`。
- `Esc` 取消；鼠标单击/双击候选也可确认。
- **`←` / `→` 也会收起列表**：和"鼠标点到别处"同义——光标移出正在拼的那个词，
  补全上下文就失效了。按键照常放行，光标该移还移。
- `Ctrl+Space` 可手动强制弹出。
- 在字符串或注释里输入不会触发。
- **支持中文命名**：中文变量/常量/过程名（如 `用户名`、`计算`）也能提取，并在输入首字时提示、补全。
- **鼠标点击其它地方自动收起**：弹窗开着时，点击弹窗以外的任意位置（包括切到别的窗口）都会自动隐藏；
  点击弹窗内的条目仍以单击/双击确认。
- **按 `←` / `→` 也自动收起**：效果同上，且按键本身会正常传给 VBE（光标照常移动）。
  收起后不会被立刻弹回来——轮询只在"行号 + 行文本"变化时才判定要不要弹。
- **作用域感知（按 VBA 官方可见性规则）**：只提示当前位置真正可见的名字——
  1. **过程级**：当前过程的局部变量和参数；其他过程里的局部变量一律不提示。
  2. **模块级 + Private/Public**：本模块的模块级名字（含 Private）都会提示；
     其他模块里只有 **Public/Global** 的才提示，
     **Private 或默认私有（模块级 `Dim`、`Const`）的不会提示**——
     所以不会出现"在模块 B 里却提醒模块 A 的私有变量"。
  3. 类模块 / 窗体 / 文档模块（ThisWorkbook、Sheet 等）的成员只能通过
     实例或限定名访问，跨模块不提示裸名。
  4. 同名遮蔽：本过程局部 > 本模块级 > 其他模块 Public。
- 依据的官方规则：模块级 `Dim`/`Static`/`Const` **默认等效 Private**；
  `Sub/Function/Property/Type/Enum` **默认 Public**；显式 `Private` 限本模块。
- **字号自动跟随 VBE**：读注册表 `HKCU\Software\Microsoft\VBA\7.1\Common` 的
  `FontFace` / `FontHeight`，换算成磅值后**减 1**（比 VBE 小一号）；每次弹出都重新读取，
  所以你在 VBE 里改了字号，弹窗会立刻跟着变。想改偏移量调 `ui.py` 里的 `FONT_DELTA`。
- **位置紧贴光标**：用 Win32 `GetGUIThreadInfo` 取 VBE 代码窗光标的屏幕坐标，
  弹窗出现在光标正下方；下方空间不够时自动翻到光标上方。
- 补全范围：当前工程所有模块里的变量、常量、Sub/Function/Property 及其参数、
  Type/Enum 及成员、Declare 声明。
- **隐式变量**：不写 `Option Explicit` 时"用到即存在"的变量（从没 `Dim` 过）也能提示，
  取自赋值目标与代码里"解释不掉"的裸标识符；默认本模块可见、跨模块不提示。

## 开机 / 打开 VBE 自动启用

每次开机都要手动 `run.bat` 太麻烦，可以把它放进开机启动：

1. 双击 **`install_autostart.bat`**：它会先装好依赖，然后在你的
   「开始菜单 → 启动」目录里创建一个指向 `pythonw main.py` 的快捷方式
   （`VbeComplete.lnk`）。
2. 之后每次 Windows 登录，工具就在后台自动运行。
3. 工具采用"焦点驱动"：只有 VBE 代码窗是前台窗口时才挂载键盘/鼠标监听，
   所以平时（你在 PyCharm、浏览器等其它程序里）它完全不装全局钩子、几乎零打扰；
   一旦你切到 VBE 写代码，它就自动生效。—— 这同时实现了"打开 VBE 时自动启用"。
4. 想取消开机自启：双击 **`uninstall_autostart.bat`** 删除该快捷方式即可。

> 说明：快捷方式指向 `pythonw.exe`，无控制台窗口、无图标，后台静默运行。

## 内存占用与资源清理（是否会占很多内存？）

- **占用很低**：工具本身是常驻的轻量 Python 后台进程，日常内存约 **30–60 MB**
  （tkinter + pynput + pywin32 的基础开销）。它不会缓存整个工程，只缓存一层
  "标识符列表"，且每 2 秒最多刷新一次，普通 VBA 工程也就几百条记录，体积可忽略。
- **VBE / Excel 关闭后会自动清理**：
  - 切出 VBE（或关闭 Excel）时，工具会立刻**卸载全局键盘/鼠标钩子**并清空标识符缓存；
  - **丢弃 Excel/VBE 的 COM 代理**（`_release_vbe_proxy`）并主动 `gc.collect()`，
    确保不留悬空的 Excel COM 引用拖住 Excel 进程；
    （注意：不是靠 `CoFreeUnusedLibraries()` —— 它释放不了**我们自己持有**的
    引用，在 Excel 关闭期间调用反而惊扰 COM，默认已不再调用。）
  - Excel/VBE 的代理还带 **1 秒 TTL**，到期主动放手，绝不长期持有；
  - 所以关闭 VBE 后，相关内存会及时回收，不会越用越多。
- 如果你怀疑有残留实例，双击 `stop.bat` 一键结束所有进程即可。

## 排错

- 找不到弹窗：先确认 Excel 的"信任对 VBA 工程对象模型的访问"已勾选。
- 确认工具在跑：任务管理器里应有 `pythonw.exe`；没有就重双击 `run.bat`。
- 依赖安装失败：在命令行运行 `python main.py`，可直接看到具体报错。
- "按 Tab 后列表框不消失"或"列表框被按 Tab 也关不掉"：
  双击 **`stop.bat`** 杀掉所有 VbeComplete 进程，再双击 `run.bat` 重启一次即可。
  这通常是因为之前残留了多个旧实例（新版已加单实例互斥体防复发）。
- **关闭工作簿 / 退出 Excel 很卡，关完 Office 又被重新拉起**：
  这是 COM 引用没及时释放的典型症状（改过 VBA 代码时尤其明显，因为 Excel 退出时
  要把 VBAProject 写回文件）。新版已有三道防线（1 秒 TTL / 宿主窗口门 /
  写完代码即放手）。若仍觉得慢，可用环境变量把工具调成最保守模式：

  ```bat
  set VBECOMPLETE_PROXY_TTL=0
  run.bat
  ```

  `=0` 表示彻底不缓存 COM 代理（每次用完即弃，最保守，代价是多一次 ROT 查询）。
  若怀疑宿主窗口探测误判（例如用 WPS 等非 Office 宿主），用
  `set VBECOMPLETE_NO_WINDOW_GATE=1` 关掉窗口门。

## 实现要点（踩过的坑，供后续迭代参考）

1. **pynput 回调绝不能 `return False`**——那是"停止监听器"的语义
   （源码 `pynput/_util/__init__.py`：`if f(*args) is False: raise StopException()`）。
   之前"只能弹一次"就是这个原因：按一次 Tab/Enter 监听器就死了。
2. **pynput 回调里抛任何异常也会停止监听器**（`_emitter` 捕获后调 `stop()`）。
   所以所有回调体都用 `try/except` 包住。
3. **要吞掉按键**（不让 Tab/Enter 传到 VBE）必须用 Windows 专用的
   `win32_event_filter` + `listener.suppress_event()`，已在本机 pynput 上验证可用。
4. 所有改 UI / 调 COM 的动作都经队列交给 tkinter 主线程，避免跨线程问题。
5. 内置看门狗：监听器若意外死亡会每 2 秒自动重启，保证能一直用。
6. **作用域（v3：模块级 + Private/Public）**。标识符记录为四元组
   `(name, module, proc, priv)`：
   - `parser.extract_records(code, module, is_std_module)` 产出完整记录；
     模块级 `Dim`/`Static`/`Const` 默认私有（`priv=True`），
     `Sub/Function/Property/Type/Enum` 默认公共，显式关键字覆盖默认；
     类/窗体/文档模块（非标准模块）的所有成员一律按"模块私有"处理。
   - `engine.filter_identifiers_by_scope(ids, proc, module)` 过滤：
     本过程局部（同模块同过程）> 本模块模块级 > 其他模块 Public，
     其他模块 Private 直接跳过。旧式二元组/纯名字列表仍兼容
     （module=None 视为全局可见），旧式数据仍兼容。
   - 光标所在模块名由 `CodeModule.Name` 实时取；
     过程名仍走 `parser.proc_at_line()` 纯文本扫描（ProcOfLine 兜底）。
   - **修复了带修饰符的 Type/Enum 声明**（`Public Enum Color` 之类）
     被误当成变量声明解析的旧 bug：`_RE_TYPE/_RE_ENUM` 改为
     `^\s*(?:(?:Public|Private|Friend|Global)\s+)*Type|Enum\s+...` 锚定。
7. **取光标位置**：pywin32 的 `win32gui` 没有 `GetGUIThreadInfo`，
   改用 `ctypes` 直接调 `user32.GetGUIThreadInfo` + `ClientToScreen`。
8. **VBE 字号**在注册表 `HKCU\Software\Microsoft\VBA\7.1\Common`：
   `FontFace`(字符串) / `FontHeight`(像素)。磅值 = 像素 / `root.winfo_fpixels("1p")`，
   可自动适配系统 DPI。
9. **"按 Tab 后弹窗不消失"的根因**（已修）：多个后台实例 + 主线程 after 队列时序。
   修法：
   - `acquire_single_instance()` 用 Windows 互斥体保证**只有一个实例**在跑（多实例会各弹一个列表框，B 实例的不会被 A 的 accept 收回）；
   - `ui.Popup` 用 `_want_visible` 标志 + `_cancel_pending_show()` 取消**已排进队列但还没执行的 `_show`** —— 否则按 Tab 收起后，刚才排在队列里的 `_show` 还会再冒出来；
   - `engine.Completer` 的 `accept()` 增加 **350ms 静默期** (`_suppress_until`)，忽略这段期间的 `trigger` 调用，避免"队列尾巴"又把它弹回来。
10. **"Tab 后光标落在词中间"的修复（坐标语义）**：根因是行内用 Tab 缩进时，
    字符位置与 VBE 的"列"可能不一致（Tab 展开成多格）。修法：
    - `vbe_bridge._vbe_measure()` 向"远超行尾"的列 SetSelection 再读回停靠列
      （字符语义被钳制到 `len+1`，显示语义钳制到"显示宽+1"，数值不同可区分），
      从而探测该行列语义与 Tab 宽度，无需预知。
    - `get_context` 把光标显示列换算成字符列（引擎的词提取/替换与行文本对齐）；
      `apply_completion` 写回后把词尾字符列换算回显示列再 SetSelection
      （并先读回实际行、以补全词为锚重新定位，兼容 VBE 对行的规范化）。
    - 相关纯函数：宽度展开 / 列换算 / 语义探测 / 整词锚定。

11. **"和 PyCharm 等程序冲突（关不掉）"的修复**：根因是之前**一直挂着
    `WH_KEYBOARD_LL` 全局键盘钩子**（Java 系 IDE 如 PyCharm/IntelliJ 退出时会等待钩子链，
    被我们的钩子卡住而关不掉）。改为**焦点驱动**：`main.py` 的 `reconcile()`
    每 300ms 检查前台窗口是不是 VBE 代码窗——是才挂载键盘+鼠标钩子，
    不是就立刻 `listener.stop()` 卸载。这样在 PyCharm/其它程序里根本不装钩子，冲突消失。
    同时这也让"打开 VBE 自动生效"自然实现（工具常驻，进 VBE 自动挂载）。
12. **中文命名标识符支持**：`engine._IDENT`、`parser._IDENT` 改为 Unicode 感知的
    `[^\W\d]\w*`（首字符字母/下划线、其余字母/数字/下划线，含中文）；
    `vbe_bridge._IDENT_TAIL` 改为 `_is_ident_tail(ch)`，用 `ch.isalnum() or ch=="_"`
    （同样含中文）。
13. **资源清理**：离开 VBE / 关闭 Excel 时，`VbeBackend.release()` 清空标识符缓存并
    丢弃 COM 代理，避免悬空引用拖住 Excel 进程、也不让内存长期占用。
    鼠标点击弹窗外自动收起由 `engine.maybe_hide_on_outside_click`
    + `ui.Popup.contains_point` 实现（全局鼠标监听只在 VBE 聚焦时挂载）。
14. **绝不长期持有 Excel 的 COM 代理**（v26，详见下面的变更记录）：代理带 1 秒 TTL，
    宿主窗口消失后不再新建，写完 VBA 代码立刻放手。这是"关文件卡 3~10 秒、
    关完又启动 Office"的根治点。

## 文件说明

- `parser.py` — VBA 标识符提取（纯文本）
- `engine.py` — 补全引擎（触发/过滤/选择/插入，纯逻辑）
- `vbe_bridge.py` — 对接真实 VBE（pywin32 COM，唯一需 Excel 的部分）
- `ui.py` — 悬浮弹窗（tkinter）
- `main.py` — 键盘监听 + 焦点判定 + 串联各模块
- `requirements.txt` / `run.bat` — 安装与启动
- `stop.bat` / `stop.ps1` — 结束后台进程（更新版本前用）
  （Windows 单实例互斥体已避免"叠多个列表框"；重启前先停再起更稳）
- `install_autostart.bat` / `install_autostart.ps1` — 加入开机启动
- `uninstall_autostart.bat` / `uninstall_autostart.ps1` — 取消开机启动

## 变更记录

- **v27（修复：开着 VbeComplete 时，关闭 VSCode / PyCharm 等其它 IDE 卡死、关不掉）**

  现象：先启动 VbeComplete（常驻监听 Excel VBE），再去 VSCode / PyCharm 写 Python，
  关闭这些 IDE 时会卡死、无法退出。

  根因：v26 里 `reconcile()` 用 `released` 标志位管理资源，逻辑跑偏——一旦离开 VBE
  超过宽限期、`released` 变成 `True`，其后每次轮询都走 `else` 分支，**把全局键盘钩子
  + 全局鼠标钩子重新挂上**，即便你正在其它程序里。`pynput` 装的是系统级
  `WH_KEYBOARD_LL` / `WH_MOUSE_LL`，只要挂着，全系统输入事件都要先经过它的钩子线程；
  别的程序退出、拆窗口时一旦被这个钩子挡住就会卡死。这恰好是 `reconcile()` 注释里
  早就警告过的「PyCharm 关不掉」坑，被 v26 的 `released` 逻辑重新踩了一遍。

  修复：把「钩子挂载 / 卸载」和「COM 释放」两条逻辑解耦。新增纯函数
  `_reconcile_resources(focused, released, backoff, last_focus_time, now, grace)`，
  **钩子是否挂载只由「焦点是否在 VBE」决定**，离开 VBE 立刻 `stop()` 拆掉，绝不因
  `released` / COM 退避而重新挂载；COM 释放仍按宽限期 / 退避处理。

  * 回归测试新增第 18 节（7 条，纯逻辑、无需 Excel），共 **120 PASS / 0 FAIL**。

- **v28（再修：ide 关闭仍卡死 —— 加「前景窗口正在关闭就不挂钩」判断）**

  现象：v27 之后，关 IDE 偶发依旧卡死。

  根因：v27 只把钩子绑定到"焦点是否在 VBE"，但 `reconcile()` 有**焦点去抖**
  （约 600ms）。关闭 IDE 时，焦点离开 VBE 的去抖窗口内仍维持 `focused=True`，
  钩子继续挂着，于是 IDE 退出流程被 `WH_KEYBOARD_LL` 钩子挡住卡死。

  修复：新增 `foreground_window_closing()`——每周期用
  `SendMessageTimeout(WM_NULL, SMTO_ABORTIFHUNG, 200ms)` 探测前台窗口是否还
  响应；正在退出的 IDE 主线程被阻塞、会超时，判定为"正在关闭"。`_reconcile_resources`
  增加 `closing` 入参，**只要检测到前台窗口正在关闭，即使去抖仍维持"在 VBE"也强制
  不挂钩**，绕过去抖窗口。前台是正常 VBE 时必然响应正常，不会误伤。

  * 回归测试：注入 tkinter/pynput 桩让第 18 节真正跑起来（之前 managed python 缺
    这两个包、第 18 节整段被 return 跳过，`_reconcile_resources` 从未被验证）。新增
    18.6（3 条，含"焦点在 VBE 但前台正在关闭 -> 不挂载"），共 **129 PASS / 0 FAIL**。

- **v29（再修：关 IDE 窗口后要 2~3 秒才真正退出）**

  现象：v28 之后 IDE 能关掉了，但关 VSCode / PyCharm 时窗口关了却要等 2~3 秒
  进程才真正退出（不是卡死，是变慢）。

  根因：关闭 IDE 时，IDE 窗口销毁后焦点会**被动落回 VBE**（Excel），`reconcile()`
  立刻判定"回到 VBE"、把系统级 `WH_KEYBOARD_LL` / `WH_MOUSE_LL` 钩子重新挂上。
  但 IDE 进程其实还在收尾（写设置、退子进程），要 1~3 秒才死，这期间被刚挂上的
  钩子拖慢——v28 的 `foreground_window_closing()` 只管"前台窗口正在关闭"那一瞬，
  焦点已落回 VBE 时它已失效，覆盖不到这种情况。

  修复：在 `reconcile()` 里记录"不在 VBE 时的前台窗口句柄"，当**进入 VBE 的瞬间**检测到
  那个窗口已被销毁（`IsWindow` 为假）→ 说明它是被关掉、焦点被甩回来的，于是设一段
  **钩子抑制冷却期**（`HOOK_SUPPRESS_SEC = 2.5` 秒），期间绝不挂载钩子；冷却结束后
  恢复正常。这样能精确区分"IDE 关掉、焦点回落"（抑制）与"用户主动 Alt+Tab 回 VBE"
  （上一个前台窗口仍活着，正常挂载、不受影响）。

  * 回归测试：第 18 节新增 18.7（4 条，含"抑制冷却期压过 focused / 不影响 COM 释放 /
    常态不误伤"），共 **133 PASS / 0 FAIL**。

- **v30（修复两个补全提示 bug）**

  1. **输入 `num` 只提醒 `num`、不提醒 `numArr`**（前缀补全被误杀）

     现象：已定义 `num` 与 `numArr`，输入首字母 `n` 两者都正常提醒，但继续输入
     `num` 却只提醒 `num`、不再提醒 `numArr`。

     根因：后端 `_collect_identifiers` 把"光标所在声明行正在声明的名字"**按名字
     整体从全部候选里剔除**（连前缀一起）。当光标停在 `numArr` 的声明行上时，
     `numArr` 被一棍子从整个标识符池删掉，于是以 `num` 为前缀再也匹配不到它。
     `extract_records` 里也曾按光标行排除同样的名字，两道关卡叠加。

     修复：解析层与收集层都**不再做声明行的全局剔除**，名字一律收录；"打全名不
     提示自己"改由引擎层精确处理——`engine.trigger` 在 `in_decl_position` 时只隐藏
     **"当前声明行正在声明的那个名字的完整拼法"**（即 `ctx["decl_names"]` 里、且
     与正在输入的词完全相等的候选）。于是：
     - 输入前缀 `num` → 同前缀兄弟 `num` 与 `numArr` **同时提醒**（bug 修好）；
     - 输入完整 `numArr`（正在声明它自己）→ 不提示 `numArr`（仍保留"打全名不提示
       自己"的既有行为，回归测试把关）。

  2. **回退键删字时"提示自己"**（误以为自己定义过该名）

     现象：用退格键删除已录入代码时，偶尔弹出的列表里只有"正在输入的那个词本身"，
     让人错觉自己真定义过这样一个名字。

     根因：当唯一剩下的候选恰好等于正在输入的词时（例如回退到只剩 `gFlag`、而
     `gFlag` 确是工程里某个名字），弹窗仍照常弹出，等于把"你刚删剩的词"当成了
     一条补全建议。

     修复：`engine.trigger` 在过滤结束后，若**唯一剩下的候选恰好等于正在输入的词
     **（`len(matches) == 1 and matches[0].lower() == word.lower()`），直接收起弹窗。
     多候选、前缀匹配不受影响——只有"没有任何别的可提示"时才安静，既避免误导，
     又保留正常的补全体验。

  * 回归测试：第 7/9 节改为"声明行名字被收录"语义（前缀补全可用，由引擎把关
    不提示自己）；第 11 节新增 4 条（Bug1 声明行 `num`/`numArr` 同屏、Bug1 打全名
    不提示自己、Bug2 回退唯一匹配等于所输词则收起、Bug2 前缀有真实匹配正常提示），
    共 **137 PASS / 0 FAIL**。

- **v31（再修两个小 bug：回退"提示自己"残留 + 列表行数/键盘选词）**

  1. **回退删字时仍会"提示自己"**（已定义 `num`、`numArr`，先输入 `numArr` 再退到
     `numA`，弹出 `numA`——但从未定义过 `numA`）

     根因：v30 只在"唯一候选正好等于所输词"时收起，挡不住**多候选**情形。退到
     `numA` 时，词本身会被当成一个候选（隐式变量"用到即存在"的残留、或光标落在
     缺口/模块识别失败时把"正在拼的词"收进池里）。此时 `numA` 与 `numArr` 一起出现，
     `numA` 也被当成一条补全建议——而它根本没被声明过。

     修复：引擎新增"真名集合"概念——只有被 `Dim/Const/Sub/Function/Type/Enum` 等
     **真实声明**过的名字才算（`VbeBackend.get_declared_names()` 提供，隐式变量不算）。
     `engine.trigger` 在**普通用法行**（`in_decl_position` 为否）时：若所输词**不在真名
     集合**里（如 `numA`），就从候选里剔除该词本身。于是：
     - 输入 `num` → 已声明的 `num` 保留，`num` 与 `numArr` 同屏；
     - 输入 `numA`（未定义）→ 只提示 `numArr`，**绝不提示 `numA`**。
     声明行里仍由 `in_decl_position` 的精确等值排除（只踢"正在起的新名字"全名）兜底，
     两层互不冲突。

  2. **列表行数 + 键盘选词**

     - **列表至少显示 8 行**：`ui.py` 新增 `MAX_ROWS = 8`，`show()` 时若匹配不足 8
       个用**空行占位**（不再随匹配数跳动）；空行占位不响应单击/双击（点了也不算确认）。
     - **回车确认**：`main.py` 的 `CONFIRM_VKS` 由 `(VK_TAB,)` 改为 `(VK_TAB,
       VK_RETURN)`，弹窗可见时 `Enter` 也接管为"确认补全"（与 ↑/↓/Tab/Esc 一并被
       `win32_event_filter` 吞掉，不会回车换行）。现在用 ↑/↓ 移到目标词后按 **Tab 或
       Enter** 都能补全，不必移动光标去点。
       若想退回到"只用 Tab"，改回 `CONFIRM_VKS = (VK_TAB,)` 即可。

  * 回归测试：第 11 节新增第 12 组，覆盖"未定义词 `numA` 在普通行被剔除、只留
    `numArr`""多候选下已定义词 `num` 仍保留""声明行打全名仍收起""回退唯一匹配收起"
    及键盘选词（↑/↓ 移动 + Tab/Enter 确认）模拟，共 **146 PASS / 0 FAIL**。

- **v32（根治回退"提示自己"+ 列表改数字键选词）**

  1. **回退快的时候还是会出现提示自身**（`numArr` 退成 `numA`，仍提示 `numA`）

     这是 v31 的**漏网**，根因有两处，叠加后才挡不住：

     - **真名集合被"正在输入的行"污染**：`declared_names` 直接取解析结果，而
       回退删字时文本里此刻**确实写着** `Dim numA`——解析层自然把它当成一条
       真实声明。于是 `numA` 进了"真名集合"，v31 那条"不是真名就剔除自身"
       的防护**直接失效**。
     - **防护只覆盖用法行**：v31 的剔除写在 `else`（非声明行）分支里，
       声明行走的是 `in_decl_position` 分支，压根不经过这条防护。
       一旦光标行判定/缓存有任何偏差，词就漏出来了。

     修复：
     - `vbe_bridge._collect_identifiers`：**真名集合剔除"光标所在声明行正在
       声明的那些名字"**（`decl_names_at_caret`）。正在敲/正在删的词不算
       "工程里已存在的真名字"，`numA` 因此永远进不了真名集合。
     - `engine.trigger`：把"不是真名就剔除自身"提为**声明行与用法行一律生效**
       的通用规则（`in_type` 类型名位置除外），不再依赖 `in_decl_position` 分支。

     现在的判定等价于 IDEA 的行为：**一个候选若恰好等于光标下正在输入的词，
     只有当这个名字在工程里"别处"确有声明时才提示**；否则一律剔除。
     于是 `num` → `num` 与 `numArr` 同屏；`numA`（从未定义）→ 只提示 `numArr`。

  2. **列表改数字键选词，去掉回车确认**

     - 候选每条前面加 **`1.` `2.` `3.` … 序号**；列表仍至少 8 行（不足补空行）。
     - **按 `1`~`9` 直接选中对应候选并写回编辑器**，不必再按确认键；
       小键盘 `1`~`9` 同样可用。数字超过候选条数时**不拦截**，照常输入数字。
     - **去掉回车确认**（`CONFIRM_VKS` 恢复为只有 `Tab`）：回车在 VBE 里是换行，
       误触发代价太大。现在 ↑/↓ 选好后**必须按 `Tab`** 才写回编辑器。
     - 序号只是显示层，确认时仍按【下标】取候选值，不会把 `1. num` 写进代码。

  * 回归测试：第 11.10 / 11.11 / 11.12 组新增 7 条，含**"修复前必然失败"的对照
    用例**（真名集合含 `numA` 时确实会漏出 `[numA, numArr]`），锁住根因；
    另含 `pick()` 正常选词、越界返回 False 且状态不变、序号显示与 8 行下限。
    共 **153 PASS / 0 FAIL**。

- **v33（回归修复：已定义变量打全名，列表反而消失了）**

  现象：定义了 `arr`，输入 `a` / `ar` 都会提示 `arr`，**但把 `arr` 打全后列表
  反而关闭了**。期望是"输入完整，提示保留"。

  根因：v30 为修"回退键提示自己"引入过一条收尾规则——**唯一剩下的候选恰好等于
  正在输入的词时直接收起弹窗**。`arr` 打全后候选只剩 `arr` 一个，正好命中，
  于是列表被关掉。这条规则当初是粗糙的"兜底"，代价就是误伤"打全名"这个最常
  见的场景：既没法 Tab 确认，也看不出自己写对了没有。

  修复：**删掉这条收尾规则**。它原本要防的"提示自己"，已由两条更精确的规则
  完整覆盖：
  1. 词**不是工程里真实声明**的名字（回退出来的 `numA` / 未定义的 `arrX`）
     → 剔除它本身（v31 + v32 的"真名集合"，含光标行声明名剔除）；
  2. **声明行上正在起的新名字** → 剔除其完整拼法（`in_decl_position` +
     `decl_names`）。

  真实存在的名字打全了就继续提示，与打前缀行为完全一致。

  * 回归测试：第 11.10b 组新增 4 条——`arr` 打首字母 / 前缀 / **全名**都保留提示，
    外加对照"未定义的 `arrX` 打全仍不提示自己"；第 11.7 条的语义同步更新为
    "回退到真实存在的名字 -> 保留提示"。共 **157 PASS / 0 FAIL**。

- **v34（新增：`←` / `→` 也收起候选列表）**

  需求：原先只有"鼠标点到别处"会收起列表，希望**按左右方向键也收起**——
  光标都移出正在拼的那个词了，补全上下文已失效，列表不该继续挂着挡视线。

  实现：在 `main.py` 的 `win32_event_filter` 里新增 `DISMISS_VKS = (VK_LEFT,
  VK_RIGHT)`，弹窗可见且焦点在 VBE 代码窗时命中即 `post(completer.hide)`。
  两个刻意的设计：
  1. **不 suppress**：按键照常传给 VBE，光标该移动还得移动（区别于
     ↑/↓/Tab/Esc——那些被钩子接管并吞掉）；
  2. **收起后不会被轮询弹回来**：`poll_editor` 只在「行号 + 行文本」变化时
     才判定要不要弹，`snapshot()` 刻意不含列号，单纯移动光标不会触发它。

  未改动 `engine.py` / `parser.py` / `vbe_bridge.py`，回归测试仍为
  **157 PASS / 0 FAIL**（键盘钩子属系统级行为，不在单测覆盖范围内）。

- **v35（根治：多字符变量打全名，列表又消失了）**

  现象：定义了 `arr`，输入 `a` / `ar` 都提示 `arr`，**把 `arr` 打全后列表关闭**。
  v33 已经修过一次，但**只有单字符变量生效、多字符的仍复现**。

  根因不是 v33 没改干净，而是**判定依据本身不可靠**。v31/v32 为了挡住"回退键
  提示自己"加的守卫是：

  ```python
  if word.lower() not in self._declared_names():   # 不在"真声明集合"里
      matches = [m for m in matches if m.lower() != word.lower()]
  ```

  它把"这个名字是否真实存在"偷换成了"解析器是否认得它的 Dim 语句"。真实工程
  里二者经常不等价：标识符可能是因为**到处在用**（隐式变量，`用到即存在`）才进
  候选池的，也可能声明在解析层没能归类的位置。于是 `arr` 明明在代码里到处出现，
  却因为不在声明集合里，被"打全名"那一刻当成自己的回声一脚踢掉 —— 前缀还在、
  全名消失，正是用户看到的现象。

  修复：**换个正确的问法**——「把光标处的词抹掉之后，这个名字在别处还存在吗？」
  * 存在 → 它是合法候选，打全照常提示（`arr` 保留）；
  * 不存在 → 它只是正在输入的回声，剔除（`numA` 不提示）。

  新增 `VbeBackend.name_exists_outside_caret(name, caret)`：只读当前模块的文本，
  用 `_blank_ident_at` 抹掉光标处的词、屏蔽字符串与注释后做一次线性扫描。
   刻意不用全量重解析（不影响标识符缓存，因此每次按键调用都很廉价）；
  已在声明集合里的名字走快速路径，连这次扫描都省掉。

  * 回归测试：**新增第 19 节 7 条**，用假 COM 对象驱动【真实的】`VbeBackend` +
    `Completer` 全链路 —— 之前的第 11 节用的是手写 `collect()` 复刻版，它会把
    caret 传进解析器、而生产代码从不传，行为不一致到足以让这类 bug 完全漏测。
    共 **164 PASS / 0 FAIL**。

- **v26（修复：关闭工作簿卡 3~10 秒、关完 Office 又被重启）**

  现象有两个特征：① **改过 VBA 代码才卡**，只改工作表内容完全正常；
  ② 关完之后 Office 会**又启动一次**。

  根因：上一版为了省一次 `GetActiveObject`，把 VBE / Excel 的 COM 代理做成了
  **模块级长缓存**。而只要我们的进程持有 `Excel.Application`（哪怕只是间接经由
  VBE 对象），Excel 就退不干净 —— 主窗口关了但 `EXCEL.EXE` 仍驻留后台，之后再打
  一次 COM 就把它"唤醒"重新弹窗（= 关完又启动 Office）。
  "改过 VBA 才慢"也正好印证：VBAProject 变脏后，Excel 退出时要把它写回文件，
  这一步要与 VBA 环境交互，被外部 COM 引用钉住就会拖上好几秒；没改 VBA 就不必
  写回，所以无感。第一版之所以没问题，是因为当时每次 `_get_vbe()` 都新建、
  用完即弃（局部变量返回即释放），从没有长期持有。

  三道防线：
  * **代理 TTL**（`VBE_PROXY_TTL`，默认 1 秒，命中不续期）：到期主动丢弃，回到
    "用完即弃"的语义，但不牺牲打字性能。
  * **宿主窗口门**（`_host_window_alive()`）：每次取代理前用纯 Win32 的
    `FindWindowW` 查 Excel 主窗口 / VBE 窗口；连续 2 次查不到就认定宿主真的退出，
    立即释放代理 + 退避，绝不再去捞 COM。带"从未见过就不拦"兜底，避免 WPS 等
    类名不匹配的宿主被误伤。
  * **写完代码即放手**：`apply_completion` 的 `finally` 里 `_release_vbe_proxy()`
    —— 刚 `ReplaceLine` 改过代码（VBAProject 变脏）时手上还攥着 CodeModule，
    而 Excel 退出时正是要写回这个工程。
  * 退避档位加码为 `1/2/5/10/30` 秒；`FOCUS_GRACE_SEC` 由 5 秒降到 2 秒，
    让"离开 VBE"能更快触发释放与降频。
  * 回归测试新增第 17 节（9 条），共 **119 PASS / 0 FAIL**。

- **v22（修复：声明 `As` 之后输入类型名会提示变量名）**

  定义变量时 `Dim x As Integer`，光标停在 `As` 之后的数据类型名位置，弹窗仍会
  给出变量名候选（例如你敲 `As Int` 会冒出 `intCount` 之类的变量）。该位置本应
  填类型名，不该提示变量名。

  * 新增 `parser.is_caret_in_type_position(line_text, caret_col)`：判断光标是否落在
    声明行里 `As` 之后的【数据类型名】位置。做法是取「光标所在的声明片段」（从
    光标往前、跳过括号、以最近的顶层逗号切分，兼容 `Dim a As Long, b As Integer`
    与 `Dim arr(0 To 9) As Long`），片段内光标前出现 `As` 即命中。
  * 先屏蔽字符串/注释再扫描，避免字符串字面量里的 "As" 误判。
  * `vbe_bridge.get_context()` 把该标记写入上下文字典 `in_type_position`；
    `engine.trigger()` 检测到该标记即静默收起弹窗（不提示任何变量名）。
  * 变量名位置（`As` 之前，如 `Dim x` 正在命名新变量）不受影响，仍照常提示。
  * `As` 只出现在 VBA 的类型语义中，因此「光标前有 As」即可稳健判定，无需枚举
    声明关键字；`Function` 参数/返回值、`Const`、`Type` 成员等位置同样生效。
  * 回归测试补 16 项断言（共 34 项）。

- **v21（修复：定义模块级/全局变量时提示其本身）**

  在声明行（`Public gCounter As Long` / `Private mTotal As String` / `Dim ...`）上
  输入时，列表里会出现正在定义的那个变量自己，输入什么都提示它。

  根因：光标排除此前只做在**隐式变量**路径（`extract_implicit_records`），
  **显式声明**路径 `extract_records` 从来没有 caret 参数，正在声明的名字
  被一边定义、一边收录进候选。

  * `extract_records` 新增 `caret` 参数；新增 `_caret_decl_names()`：
    光标位于变量/常量声明行时，返回**该行声明的全部名字**并在收录时跳过。
  * 必须排除**整行**而非"光标处那一个词"——输入 `g` 时光标处的词是 `g`，
    而声明的变量是 `gCounter`，只比对整词仍会自我提示。
  * 只在声明行生效：使用行不受影响，v15「打全变量名后列表保留」的行为不变；
    caret 取不到时不排除，避免误伤。
  * 回归测试补 6 项断言（共 18 项）。

- **v20（修复：光标词排除错位导致的"自我提示"）**

  真实工程（UserForm3 第 61 行）输入 `n` 时列表里出现 `n` 自己。日志定位到
  根因：`extract_implicit_records` 的顺序是 **先 mask 再按列号抹光标词**，
  但 `_mask_strings_and_comments` 会把字符串压缩成少量空格、**不保证等长**
  （实测 `"Scripting.FileSystemObject"` 28 字符 -> 2 空格，全文件 18 行长度
  不一致、最多差 26 字符）。于是 VBE 给的列号（基于原始文本）在 mask 文本上
  整体错位，`_blank_ident_at` 抹错了位置，正在输入的词被当成隐式变量收录。

  * 改为**先在原始文本上按列号抹光标词，再 mask**——列号与 VBE 完全一致。
  * `_blank_ident_at` 增加列号越界钳制（宿主差异导致列号 > 行长时钳到行尾）；
    不做"回退一格"猜测，因为 `c-1 == ident.end()` 无法区分"正在输入"与
    "已打完且后面有空格"，猜测会误抹已有标识符。

- **v19（加入诊断日志与回归测试）**

  为定位"某个变量为什么不提示"这类问题，补上了可开关的诊断能力（**默认关闭**）：

  - `log.py` + `run_debug.bat` / `show_log.bat`：设 `VBECOMPLETE_LOG=1` 才写
    `VbeComplete.log`。日志第一行记录**实际运行目录与各 .py 的修改时间**（排
    "改了文件却没生效"最有用），其后记录模块收集、光标位置、光标所属过程，
    以及候选命中的**原始记录（含模块/过程作用域）**——可直接区分"压根没收录"
    与"收录了但被作用域过滤"。
  - `tests/test_real_project.py` + `run_tests.bat`：用真实工程（脚本.xlsm 导出的
    14 个模块，固化在 `tests/testdata`）做回归，覆盖跨过程隔离、跨模块 Private、
    自我提示、打全名保留、中文变量、多层嵌套等 12 项断言，不依赖 Excel。
  - 用真实工程验证：`UserForm3` 主过程第 60/61/63 行输入 `n`，候选均为
    `['num', 'numColInTargetSheet']`，解析层判定正确。

- **v18（修复：自我提示 + 旧版调用方兼容）**

  1. **自我提示（打 `n` 提示 `n`）**：`_caret_position` 取模块名用的是
     `CodeModule.Name`，而收集侧比对的是 `VBComponent.Name`，两者在部分宿主里
     不一致 → 光标位置被判定为"取不到" → 光标处的词被当成隐式变量收录。
     改为优先取 `CodeModule.Parent.Name`（与 `comp.Name` 同源），取不到模块名时
     宁可对全部模块应用也不丢弃；比对改为忽略大小写。
  2. **旧版调用方兼容**：`extract_implicit_records` 收到旧式的"纯名字序列"
     （无法携带作用域）时，改为按本模块重新计算声明，不再一律视为全局已声明。
     这样即使 `vbe_bridge.py` 没同步升级，也不会再出现"被别处同名参数挡掉"。

- **v17（修复：其它过程的同名参数挡掉本过程的变量）**
  - 现象：主过程里用到 `num`，而同模块另一个 `Private Function f(ws, num)` 有同名
    参数时，主过程内（尤其深层 `For`/`If` 嵌套处）输入 `n` 提示不出 `num`。
  - 根因：收集隐式变量时传入的"已声明名字"是一个**不区分作用域的扁平集合**，
    只要同名就一律跳过。本过程的 `num` 因此从未被收录，而唯一那条 `num` 记录
    归属那个函数，在主过程里不可见。
  - 修复：`declared` 改为**按作用域精确排除** —— 模块级声明全模块排除，**过程级
    声明（Dim / 参数）只在同名过程内排除**；调用侧改传完整的 `extract_records`
    记录（仍兼容旧的纯名字序列）。
  - 验证：主过程输入 `n` → `num`、`numColInTargetSheet`；参数 `num` 在其所属函数内
    照常可见；不泄漏到无关过程；v16 的跨过程隔离与多层嵌套均无回归。
- **v16（修复：隐式变量跨过程泄漏）**
  - 现象：函数 A 里定义了 `num`，函数 B 里定义了 `num`、`num2`；在函数 A 输入 `n`
    会提示出 `num2`（本应只提示 `num`）。
  - 根因：`vbe_bridge.py` 的 `IMPLICIT_SCOPE = "module"`，把所有"未声明变量"
    （不打 `Option Explicit` 时由赋值/For 循环产生的变量）一律记为模块级，
    于是函数 B 的 `num2` 在整个模块可见，泄漏到函数 A。
  - 修复：
    1. `IMPLICIT_SCOPE` 改为 `"proc"`：赋值/For/For Each 产生的变量按"首次出现
       所在过程"打标签，函数间互不干扰。
    2. `extract_implicit_records` 的去重由"模块级同名合并"改为"按 名字+过程 分别
       记录"，否则同名变量只记到首次出现的过程，导致另一函数里自己定义的同名
       变量反而提示不出来（反向 bug）。
    3. 只读用法（只被读取、从未赋值）的隐式变量仍记为模块级，避免"松散全局变量"
       因循环结束后的陈旧过程标签而丢失；同时排除了由此误打标签的风险。
  - 验证：函数 A 输入 `n` → 仅 `num`；函数 B 输入 `n` → `num`、`num2`；
    中文变量、Option Explicit+Dim、跨模块 Public/Private 隔离均不受影响。

- **v15（打全变量名后列表不再消失）**
  - 现象：变量名打到一半会弹列表，但一打全（如把 `cell` 打完整），候选列表立刻消失。
  - 根因：`engine.trigger` 里对"多字母且正好等于某候选"的情况做了排除
    （`allow_exact` 仅对单字母放开），导致打全后候选只剩自身被排除 → 列表变空 → 隐藏。
  - 修复：候选改为"所有以当前词为前缀的标识符"，**包含完全相等项**；
    打全名与打首字母行为一致——只要当前词仍是某候选的前缀，列表就保留，
    直到 Tab 确认 / 鼠标点列表外 / 改成不再匹配的词。
    默认选中项在"词恰好等于某候选"时优先选中该完整项，Tab 即确认当前已输入名字。
  - 单字母（`i`）本就因放开 `allow_exact` 可提示，此次一并统一为"前缀匹配"逻辑。

- **v14（清理：移除最后一段测试演示代码）**
  - 删除 `parser.py` 末尾 `if __name__ == "__main__":` 下的 VBA 样例与
    `print` 输出块（约 59 行），该文件现在是纯逻辑模块。
  - 至此全项目**零日志、零测试、零调试代码**：不写日志文件、不打印到控制台、
    无 `test_*.py` / `diag_*.py` / `run_debug.bat`。
  - 仅保留的 `__main__` 是 `main.py` 的程序入口（`main()`），属于核心功能。

- **v13（修掉"打什么就提示什么"的自我提示）**
  - 现象：单个字母（`i`）或单个汉字（`我`）即使从未定义、也从未使用过，
    一输入就弹出它自己。
  - 根因：v11 的"隐式变量"提取会把**正在输入的那个词**也收录进去——
    此刻代码里确实出现了 `i`，解析器便认为"它被用过"，于是自我提示。
    多字母时因为匹配会排除"等于自身的候选"而看不出来，单字母为了能提示
    特意放开了这条限制，问题就暴露了。
  - 修复：解析时**排除光标所在的那个词**。
    - `parser._blank_ident_at()`：把光标处的标识符抹成等长空格（保持偏移不变），
      后续赋值判定与用法扫描都自动跳过它；
    - `vbe_bridge._caret_position()`：只读 `GetSelection` 取光标位置
      （**绝不 SetSelection**，避免在 WPS 上重演光标乱跳），并把显示列换算成字符列；
    - 列语义判定不了时（一行中间输入中文等场景）宁可放弃排除，也不抹错词。
  - 效果：没定义、也没用过的词不再提示；只要在别处用过（哪怕只是 `For i`）
    就照常提示。
- **v5（修复回归）**
  - 修复「上下键不能选词」：焦点驱动重构后遗留的 bug——`win32_filter` 调用
    `suppress_event()` 时取错了监听器状态键（写的 `kbd`、读的 `listener`），
    导致按键未被吞掉、方向键把弹窗直接收起。已统一为 `state["kbd"]`，并在
    `on_press` 增加 `_VK_HANDLED_WHEN_VISIBLE` 防御，避免 ↑/↓/Tab/Esc 被误收起。
  - 修复「中文变量名不提示」：VBE 全角字符（中文）占 2 列，`get_context`
    原本只在行内含 Tab 时做显示列→字符列换算，中文行列错位数导致切词错误。
    现已对含全角字符的行也做换算（`_vbe_measure` 能探测 wide2 语义）。
- **v11（支持"没定义也能提醒"的隐式变量）**
  - 需求：不写 `Option Explicit` 时变量"用到即存在"，没有 `Dim` 也要能提示。
  - 新增来源 `parser.extract_implicit_records`，两条判定：
    1. **被写入过**：赋值目标（`x = ` / `Set x = ` / `Let x = ` / `x(i) = `）、
       `For i = `、`For Each c In`；含单行 `If ... Then y = 1 Else z = 2`。
    2. **只被读取过**：把代码里所有"解释不掉"的裸标识符收进来（VBA 里凡不是
       关键字/内置符号、又没有任何声明的裸标识符就是隐式变量），排除成员访问
       `.xxx`、调用/下标 `xxx(`、命名参数 `xxx:`、`As/New/Lib/Alias` 后的类型名。
  - 去重：已声明的名字不再作为隐式变量收录（避免覆盖 `extract_records` 的准确作用域）。
  - 作用域：默认"模块级 + 仅本模块"——**本模块任意位置可提示、跨模块不提示**。
    想改成严格过程级（只在首次出现的过程内提示，更贴近 VBA 语义），把
    `vbe_bridge.py` 的 `IMPLICIT_SCOPE` 改成 `"proc"`；想完全关掉，把
    `ENABLE_IMPLICIT_IDENTIFIERS` 改成 `False`。
  - 顺带修了 `Declare PtrSafe Function`（64 位 Office 的 API 声明）不被识别。
- **v10（WPS 下光标自己跳到代码中间）**
  - 现象：一行写一半停一会儿，再输入或移动鼠标，光标跳到代码中间。
  - 根因：判定 VBE"列语义"时要探测——把光标临时推到 `列=行尾+100` 再读回停靠列，
    然后还原。Office 能正常还原；**WPS 的 VBA 兼容编辑器对 `SetSelection` 的处理
    不同（越界列不钳制 / 还原异步）**，探测列没被还原，光标就留在了别处。
  - 修复（三级，越靠前越安全）：
    1. **被动判定优先**：字符列语义下 VBE 报告的列不可能超过 `len+1`；若
       `ec > len+1` 则必然是显示列，直接拟合即可——**完全不移动光标**。
       打字时光标几乎总在行尾，这条覆盖了绝大多数输入。
    2. **会话缓存**：列语义是编辑器级属性，探测一次长期复用，不再反复动光标。
    3. 仍要探测时走 `_probe_semantics`：`try/finally` 保证还原 + 还原后读回校验
       （不符再试，最多 3 次）；**校验始终失败则标记 `probe_broken`，此后永久禁用
       探测**，退回被动判定，杜绝光标乱跳。
- **v12（精简工程）**
  - 彻底移除日志相关代码：`main.py` 的 `log()` / `say()` / `_boot_msg`、
    `vbe_bridge.py` 的 `_log()` / `_dbg()`、`engine.py` 的 `debug_hook` 与
    `_dbg()`，以及 `LOG_ENABLED` 等开关。程序不再产生任何日志文件。
  - 删除与运行无关的辅助文件：`test_engine.py`、`diag_pynput.py`、`run_debug.bat`。
  - 想看报错时，在命令行运行 `python main.py` 即可（带控制台输出）。
- **v9（中文不提示的真正根因：显示列当字符列用 -> IndexError）**
  - 现象：英文行正常取词，中文行一律取不到上下文（内部为列越界导致的异常）。
    `get_context failed: IndexError('string index out of range')` -> `ctx=None -> hide`。
  - 根因：`get_context` 里**先用 VBE 原始列 `ec` 调 `_classify` 索引行文本，
    之后才做显示列→字符列换算**。VBE 的列是**显示列**（中文等全角占 2 格），
    而 `line_text` 是字符序列：`'    我们'` 只有 6 个字符、显示宽却是 8，
    光标在行尾时 `ec=9`，`line_text[6]` 越界抛 IndexError，整个上下文返回 None。
    纯英文行显示列==字符列，所以从不触发——正好解释"英文行、中文不行"。
  - 修复：**先换算列再判定字符串/注释**；`_classify` 内部再对 `caret_col`
    做钳制兜底，保证永不因越界抛异常；异常分支也统一钳制到 `len(line)+1`。
- **v8（中文开头仍不提示：轮询被"焦点依赖"卡死）**
  - 根因：v7 引入的轮询被两处焦点逻辑卡住，而这**只影响中文**——
    ① 轮询被 `vbe_focused` 门控：输入中文时**输入法的候选/组字窗口会短暂
    抢走前台窗口**，`in_vbe_code_pane()` 判定为 False，轮询整个停摆；
    ② 每次焦点变化都把基线 `last_snap` 清空，等焦点回到 VBE，刚提交的中文
    被当成"首次记录"只记录不触发 —— 中文的变化被彻底吞掉。
    英文不走输入法，没有 IME 窗口，所以一直正常。
  - 修复：**轮询不再受焦点门控**（轮询只做 COM 读取，不像全局钩子那样干扰
    其它程序，可常开；VBE 不可用时 `snapshot()` 返回 None 自然空转）；
    **焦点变化不再清空基线**；并给焦点判定加**去抖**（连续 2 次读数一致，
    约 600ms，才认定焦点真的变了），避免 IME 窗口导致误判"离开 VBE"。
  - 附带修复「刚声明的变量不提示」：标识符缓存有 2 秒 TTL，刚敲完
    `Dim 用户名` 就打 `用` 时缓存里还是旧内容。现文本一变化就让缓存失效，
    并限流为最快 0.4 秒重解析一次（避免每敲一键都全量解析所有模块）。
  - 新增「刚弹出保护期」(0.15s)：输入法组字产生的杂散按键不会立刻关掉
    刚弹出的列表，真正的判定由轮询重新给出。
- **v7（细节打磨：中文开头 / 中文补全身后光标 / 单字母）**
  - 修复「中文开头的变量不提示」——改为**轮询编辑器内容变化**触发补全，
    不再依赖键盘事件。原因：中文经输入法(IME)输入时，键盘钩子在 `on_release`
    往往只能拿到 `VK_PROCESSKEY` 或空字符，导致中文永不触发；直接读编辑器
    文本则与输入法无关，中英文、粘贴一律可靠。轮询仅在 VBE 聚焦时进行，
    且只在「同一行内文本变化」时触发（换到别的行、只移动光标都不弹，避免打扰）。
  - 修复「英文开头+中文的变量，Tab 补全后光标落在名字中间」——
    `apply_completion` 的列语义探测原先在 **ReplaceLine 之前的旧行**上做，
    而用户往往只打了首字母（纯 ASCII），探测被跳过、默认"字符列"；补全后的
    新行含中文、VBE 按显示列计（中文占 2 格），于是少算格差、光标落词中。
    现改为**基于写回后的实际行**探测，并给无法复现时的兜底加上"含全角则按 2 列"。
  - 修复「单字母变量（i / j / n）不提示」——匹配时原先一律排除"等于自身的
    候选"，单字母打完就等于自身，故永远无候选。现单字母允许列出自身，
    多字母保持排除（避免打完完整变量名后还挂着无意义弹窗）。
  - 顺带修正 v6 的一处笔误：`VK_PROCESSKEY` 判断写反（本该跳过却执行了隐藏）。
- **v6（真正修复中文不提示的根因）**
  - v5 对「中文变量名不提示」的判断**不完整**。列换算本身正确，真正的根因在
    `main.py` 的**事件路由**：补全触发原本只写在 `on_release`（按键抬起）。
    但中文经输入法（IME）以 `WM_CHAR` 消息在**按下阶段**送达 `on_press`，
    其 `on_release` 拿到的是 `VK_PROCESSKEY` 或空字符，于是中文永不触发
    `trigger`，自然不弹列表（英文是普通按键，按下/抬起都带字符，所以正常）。
  - 修复：把「标识符字符触发补全」同时放到 `on_press`（覆盖 IME 中文），
    并让 `on_press` 的收起分支跳过 `VK_PROCESSKEY(229)`，避免 IME 组字过程
    误关弹窗。英文在按下/抬起都触发，双重触发无害（只是刷新同一份候选）。
- **v4**：鼠标点击外部自动隐藏、中文命名支持、修复 PyCharm 冲突（焦点驱动）、
  开机/开 VBE 自动启用、内存清理与说明。
- **v3**：作用域区分（跨模块 Private 不提示、本过程局部优先）。
- **v2**：焦点/光标定位、确认键仅 Tab。
- **v1**：初始可用版本（输入即弹、Tab 补全、↓/↑ 选择、Esc 取消）。


---

## 如何确认新版本已生效

Python 在**启动时**把 `.py` 读进内存，只替换文件而不重启进程，跑的仍然是旧代码。
更新后请务必：`stop.bat` → 覆盖文件 → `run.bat`。

若行为与预期不符，先确认运行的目录里确实是新文件。在**程序实际所在目录**执行：

```bat
findstr /C:"declared_by_proc" parser.py
findstr /C:"CodeModule.Parent.Name" vbe_bridge.py
```

两条都有输出即为 v18；没有输出说明覆盖到的不是新文件（常见于把工具装在别处，
只覆盖了工作区副本）。最省事的做法：**删除旧目录，重新解压 `VbeComplete.zip`**。


---

## 排障：某个变量为什么不提示

1. **先跑回归测试**（不需要 Excel）：双击 `run_tests.bat`。
   全部 PASS 说明解析层没问题，问题在运行时。
2. **再开诊断日志**：`stop.bat` → 双击 `run_debug.bat` → 在 VBE 里复现一次
   → 双击 `show_log.bat`。

日志里重点看这几行：

```
boot: 运行目录=...              <- 确认 Python 跑的是不是你刚覆盖的那个目录
  parser.py  mtime=...          <- 确认文件是新的（时间应为你覆盖文件的时刻）
collect: caret_mod=... caret=... <- caret 为 None 说明没拿到光标（会自我提示）
  mod=UserForm3 ... caret_applied=True
trigger: ... word='n' proc=... module=...
  raw hits for 'n': [...]        <- 关键字原始记录：看它属于哪个模块/过程
```

- `raw hits` 里**没有**目标变量 → 解析层没收录（看它是赋值语句还是只读用法）
- `raw hits` 里有、但 `matches` 没有 → 被作用域过滤掉了（过程/模块不匹配）
- `caret_applied=False` 或 `caret=None` → 会出现"打什么提示什么"的自我提示

日常使用请运行 `run.bat`，**不产生任何日志文件**。
